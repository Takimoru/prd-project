# Development Dockerfile for Express + TypeORM + SQLite
# Per PRD: Container + CI setup with pnpm

FROM node:18-alpine

# Install pnpm
RUN npm install -g pnpm

# Set working directory
WORKDIR /app

# Copy package files
COPY package.json pnpm-lock.json* ./

# Install dependencies
RUN pnpm install

# Copy source code
COPY . .

# Create uploads directory
RUN mkdir -p uploads

# Expose port
EXPOSE 4000

# Development command with hot reload
CMD ["pnpm", "run", "dev"]


