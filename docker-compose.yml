version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    ports:
      - "4000:4000"
    env_file:
      - packages/server/.env
    restart: always

#  client:
#    build:
#      context: .
#      dockerfile: packages/client/Dockerfile
#    ports:
#      - "5173:5173"
